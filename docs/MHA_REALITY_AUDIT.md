# MHA Problem Statement — Reality Audit & Production Verification

**System:** Intelligent Border Video Analytics Platform (IBVAP)  
**Host Environment:** Windows 11 AMD64, Intel Core i3 (CPU-Only, No Discrete NVIDIA GPU), 12GB RAM  
**Audit Date:** 2026-09-06 (Grand Finale Verification)  
**Status Key:**
- **VERIFIED**: Functionality is genuinely working, verified via live CCTV stream, automated regression tests, or execution traces.
- **PARTIAL**: Architecture and implementation exist; partially validated or operating in development/heuristic mode.
- **NOT_VERIFIED**: Architecture and modules defined, but unverified due to lack of domain-specific footage or required hardware.
- **NOT_IMPLEMENTED**: Intentionally not built or deferred.

---

## 1. MHA Problem Statement Requirement Mapping

| # | MHA Requirement | Category | Code & Architecture Location | Empirical Test Evidence | Honest Limitations & Boundaries | Next Step |
|---|---|:---:|---|---|---|---|
| **1** | **Integration with existing IP CCTV / RTSP cameras** | **VERIFIED** | `services/ingestion/rtsp_source.py`, `services/ingestion/camera_manager.py`, MediaMTX gateway (`configs/cameras.yaml`) | MediaMTX RTSP stream live at `rtsp://127.0.0.1:8554/CAM-01` (decoded at 592x360 H.264 at 7.11 FPS). Camera health state transitions (`ONLINE -> RECONNECTING -> ONLINE`) verified without process termination. | Relies on RTSP/HTTP endpoints configured in YAML; dynamic ONVIF discovery is not implemented. | Add WS-Discovery / ONVIF automated device discovery daemon. |
| **2** | **Real-time human detection** | **VERIFIED** | `services/detection/yolo_adapter.py`, `services/detection/detection_engine.py` | Executed live against video frames: detected `person` with bboxes, confidence, and latency (40-75ms on CPU). 4/4 pytest detection tests pass. | Using COCO general pretrained YOLOv8n weights; not fine-tuned on military/border camouflage uniforms. | Fine-tune YOLOv8n on annotated border security footage. |
| **3** | **Real-time human tracking with persistent IDs** | **VERIFIED** | `services/tracking/bytetrack_adapter.py`, `services/tracking/track_analytics.py` | 34/34 tracking pytest tests pass. 4 unique persistent tracks maintained over video sequence; speed, direction (E, NE), dwell time, and ground trajectory computed. | Pure IoU/ByteTrack association without DeepSORT appearance embeddings; identity switches possible during prolonged occlusions. | Incorporate lightweight visual Re-ID feature extractor for occlusions. |
| **4** | **Real-time vehicle detection** | **VERIFIED** | `services/detection/yolo_adapter.py` | Detections produced for `vehicle` (mapped from COCO car, truck, bus, motorcycle); bboxes and track trajectories logged live. | Uses COCO vehicle classes; lacks custom border patrol vehicle definitions. | Add domain dataset for specialized patrol and transport vehicles. |
| **5** | **Vehicle classification (subtypes)** | **PARTIAL** | `services/detection/yolo_adapter.py`, `services/tracking/schemas.py` | `subclass` field populated with `car`, `truck`, `bus`, `motorcycle`. | Subclassification limited to standard COCO vehicle categories; fine-grained classes (e.g. military patrol, armored carrier, tanker) not trained. | Train custom secondary classification head using NVIDIA TAO. |
| **6** | **Automatic Number Plate Recognition (ANPR)** | **PARTIAL** | `services/anpr/plate_detector.py`, `services/anpr/ocr_adapter.py`, `services/anpr/anpr_pipeline.py` | 9/9 pytest ANPR tests PASS (mock detector, text cleanup, multi-frame consensus). | **REAL PLATE RECOGNITION IS NOT_VERIFIED**: 30s test footage contains low-res, distant vehicles (<35px plate width). System emits `UNREADABLE` without hallucinating fake plates. | Benchmark against 1080p dedicated gate footage with readable plates. |
| **7** | **Virtual fence & restricted zone enforcement** | **VERIFIED** | `services/rules/rule_engine.py`, `services/rules/geometry.py` (`configs/zones.yaml`) | `geometry_ground_contact` test verified: bottom-center ground anchor (x_center, y2) prevents false upper-body alarms. 3/3 pytest rules tests pass. | Zone coordinates configured via static YAML configuration; no dynamic interactive polygon drawing in UI. | Connect React polygon canvas directly to zone update API. |
| **8** | **Intrusion detection & real-time alerting** | **VERIFIED** | `services/rules/temporal_event_engine.py`, `services/incidents/event_correlator.py`, FastAPI WebSocket (`/ws/alerts`) | Live detection script emitted `ZONE_INTRUSION` and `REPEATED_ENTRY` events with full reasons. Real-time WebSocket pushes to React dashboard. | Notification queue is in-memory; alerts dropped if client disconnected during burst. | Implement Redis Pub/Sub backed persistent alert queue. |
| **9** | **Suspicious / unusual activity detection** | **PARTIAL** | `services/rules/temporal_event_engine.py`, `services/rules/activity_engine.py` | `REPEATED_ENTRY`, `LOITERING`, and `DIRECTION_VIOLATION` rules implemented with configurable cooldowns and de-duplication. Unit verified. | Heuristic/rule-based thresholds only (e.g., dwell time > 20s, entry count >= 3). No ML-based visual anomaly detection. | Train spatio-temporal autoencoder for unsupervised movement anomaly scoring. |
| **10** | **Night-time movement detection** | **PARTIAL** | `services/preprocessing/clahe_enhancer.py`, `services/rules/temporal_event_engine.py` | `process_night_movement()` tags events with `[NIGHT]` based on configurable timestamp window. CLAHE contrast enhancer implemented. | Rule is a clock-based heuristic; model has not been trained on thermal or IR night-vision datasets. | Evaluate dedicated infrared/thermal object detection model. |
| **11** | **Face detection** | **NOT_IMPLEMENTED** | `services/detection/detection_engine.py` (abstract architecture supports pluggable detectors) | No face detection model running in inference pipeline. | Deferred due to privacy constraints, absence of high-res facial camera feeds, and core perimeter focus. | Implement RetinaFace / InsightFace adapter behind privacy masking switch. |
| **12** | **Multi-camera management & scalability** | **VERIFIED** | `services/ingestion/camera_manager.py`, `apps/backend/routers/cameras.py` | Multi-camera ingestion and monitoring verified. Camera health transitions verified without crash. | No cross-camera target association (MTMC); single-node architecture tested with active streams. | Add multi-camera feature vector matching for cross-camera handover. |
| **13** | **Command and control dashboard** | **VERIFIED** | `apps/frontend/` (React 18 + Vite + TypeScript) | Frontend compiles cleanly (`npm run build`, 0 errors). Live video, SVG bounding box overlays, and incident management verified. Prioritizes primary CCTV camera. | Role-based UI components not hidden in UI views. | Wire frontend navigation guard to user JWT token role. |
| **14** | **Incident lifecycle management** | **VERIFIED** | `apps/backend/routers/events.py`, `apps/backend/database/models.py` | Full 4-state lifecycle (`NEW` -> `ACKNOWLEDGED` -> `INVESTIGATING` -> `RESOLVED`) verified via automated REST execution against SQLite DB. | Operator assignments are single-tier; no escalation timer for unacknowledged critical incidents. | Add automated escalation scheduler for unacknowledged incidents. |
| **15** | **Evidence capture with cryptographic integrity** | **VERIFIED** | `services/evidence/collector.py`, `services/evidence/hash_chain.py`, SQLite `evidence_hash_chain` table | 395 real evidence frames in `data/evidence/`. SHA-256 integrity check verified. Modifying 1 byte triggers immediate tamper detection failure in `verify_chain()`. | Evidence stored on local filesystem; no off-site backup or cloud replication. | Implement scheduled NAS/S3 signed evidence sync. |
| **16** | **Cybersecurity & platform hardening** | **VERIFIED** | `apps/backend/auth/jwt_auth.py`, `apps/backend/auth/rbac.py`, `apps/backend/routers/auth_router.py` | Defensive headers verified. Login endpoint issues valid HMAC-SHA256 JWT tokens. Invalid/expired tokens rejected with 401. RBAC viewer blocked from camera deletion with 403. Path traversal rejected with 404. | SQLite development database; production requires PostgreSQL connection pool. | Provision PostgreSQL server for enterprise production deployments. |
| **17** | **Offline / edge operation capability** | **VERIFIED** | Standalone architecture (MediaMTX + FFmpeg + FastAPI + SQLite + Vite) | System functions 100% offline on a local subnet with zero internet dependency or cloud calls. | Validated on bare-metal Windows x64; not yet packaged into container image for NVIDIA Jetson. | Create ARM64 Docker container for NVIDIA Jetson Orin. |

---

## 2. Advanced / Hardware Expansion Attributes

| Attribute | Status | Empirical Findings |
|---|:---:|---|
| **NVIDIA GPU Acceleration** | **NOT_VERIFIED** | Decoupled adapter architecture ready (`TensorRTAdapter`, `OpenVINOAdapter`, `ONNXRuntimeAdapter`). Physical workstation is CPU-only (Intel i3, 0 CUDA GPUs). No benchmarks fabricated. |
| **Distributed Blockchain** | **NOT_IMPLEMENTED** | The platform uses a **tamper-evident cryptographic evidence hash chain**, NOT a blockchain. Claiming blockchain would be technically dishonest. |
| **Continuous Learning / MLOps** | **PARTIAL** | Dataset frame collection script (`scripts/collect_dataset_frames.py`), DVC adapter stub, and MLflow adapter stub exist. Active training loop requires domain annotations. |
