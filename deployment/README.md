# IBVAP — Deployment Directory

## Structure

```
deployment/
  single_node/      ← Single-server deployment (current dev + near-term prod)
  nvidia/           ← NVIDIA GPU / DeepStream deployment path
  edge/             ← Jetson / edge deployment
  configs/          ← Environment-specific configuration templates
```

## Status

| Path | Status |
|------|--------|
| `single_node/` | PARTIAL — template provided |
| `nvidia/` | PLANNED — see docs/NVIDIA_DEPLOYMENT.md |
| `edge/` | PLANNED — see docs/EDGE_DEPLOYMENT.md |

See `docs/DEPLOYMENT_GUIDE.md` for step-by-step instructions.
